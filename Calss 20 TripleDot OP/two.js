let eids = [101,102,103,104]
//Create new Array based on existing

let uids = []

//using for loop
for (let index=0 ; index <= eids.length-1 ; index++){
    uids.push(eids[index])
}

console.log(uids)